name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          cd backend
          pip install -r requirements.txt

      - name: Create Lambda Layer
        run: |
          cd backend
          mkdir -p python
          pip install \
            --platform manylinux2014_x86_64 \
            --target=python \
            --implementation cp \
            --python-version 3.9 \
            --only-binary=:all: \
            --upgrade \
            -r requirements.txt
          zip -r layer.zip python

      - name: Upload Lambda Layer
        uses: actions/upload-artifact@v3
        with:
          name: lambda-layer
          path: backend/layer.zip

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download Lambda Layer
        uses: actions/download-artifact@v3
        with:
          name: lambda-layer

      - name: Deploy to AWS Lambda
        run: |
          aws lambda update-function-code \
            --function-name my-lambda-function \
            --zip-file fileb://backend/layer.zip \
            --publish
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AKIAQE43KHJGLLUWGNUY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.hPPb1SbTkQcXnLeC00x/XLNn5r3lqcTVCMTUNim9 }}
          AWS_REGION: 'us-east-1'
